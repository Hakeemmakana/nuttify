<%-include("../../views/partials/user/header")%>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About - Nutify</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            color: #333;
            line-height: 1.6;
        }

        .container1 {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .breadcrumb {
            margin-bottom: 40px;
            font-size: 14px;
            color: #666;
        }

        .breadcrumb a {
            color: #666;
            text-decoration: none;
        }

        .breadcrumb a:hover {
            color: #dc3545;
        }

        .breadcrumb span {
            margin: 0 8px;
        }

        /* Statistics Section */
        .stats-section {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 60px;
        }

        .stat-card {
            background: white;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            padding: 30px 20px;
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .stat-card.featured {
            background: #dc3545;
            color: white;
        }

        .stat-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 24px;
        }

        .stat-card .stat-icon {
            background: #000;
            color: white;
        }

        .stat-card.featured .stat-icon {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .stat-number {
            font-size: 32px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .stat-description {
            font-size: 14px;
            color: #666;
        }

        .stat-card.featured .stat-description {
            color: rgba(255, 255, 255, 0.9);
        }

        /* Story Section */
        .story-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            margin-bottom: 80px;
        }

        .story-content h2 {
            font-size: 48px;
            font-weight: bold;
            margin-bottom: 30px;
            color: #333;
        }

        .story-content p {
            font-size: 16px;
            line-height: 1.8;
            color: #666;
            margin-bottom: 20px;
        }

        .story-image {
            position: relative;
            background: linear-gradient(135deg, #ff6b9d, #c44569);
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            min-height: 400px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .nutify-logo {
            font-size: 72px;
            font-weight: bold;
            color: #8B4513;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.3);
            transform: rotate(-10deg);
            font-family: 'Comic Sans MS', cursive;
        }

        /* Services Section */
        .services-section {
            background: white;
            border-radius: 15px;
            padding: 50px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.05);
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 40px;
        }

        .service-item {
            text-align: center;
        }

        .service-icon {
            width: 80px;
            height: 80px;
            background: #000;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: white;
            font-size: 32px;
        }

        .service-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
        }

        .service-description {
            font-size: 14px;
            color: #666;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .stats-section {
                grid-template-columns: repeat(2, 1fr);
                gap: 15px;
            }

            .story-section {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .story-content h2 {
                font-size: 36px;
            }

            .services-grid {
                grid-template-columns: 1fr;
                gap: 30px;
            }

            .container1 {
                padding: 15px;
            }

            .nutify-logo {
                font-size: 48px;
            }
        }

        @media (max-width: 480px) {
            .stats-section {
                grid-template-columns: 1fr;
            }

            .stat-card {
                padding: 20px 15px;
            }

            .story-content h2 {
                font-size: 28px;
            }
        }
       
    </style>
</head>
<body>
    <div class="container1">
        <!-- Breadcrumb -->
        <nav class="breadcrumb">
            <a href="/" >Home</a>
            <span>/</span>
            <span>About</span>
        </nav>

        <!-- Statistics Section -->
        <div class="stats-section">
            <div class="stat-card">
                <div class="stat-icon">🏪</div>
                <div class="stat-number">10.5k</div>
                <div class="stat-description">Product variants online</div>
            </div>
            
            <div class="stat-card featured">
                <div class="stat-icon">💰</div>
                <div class="stat-number">33k</div>
                <div class="stat-description">Monthly Product Sales</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-number">25.5k</div>
                <div class="stat-description">Customers active in our site</div>
            </div>
            
            <div class="stat-card">
                <div class="stat-icon">💵</div>
                <div class="stat-number">400k</div>
                <div class="stat-description">Annual gross sale in our site</div>
            </div>
        </div>

        <!-- Story Section -->
        <div class="story-section">
            <div class="story-content">
                <h2>Our Story</h2>
                <p>Launched in 2015, Nutify is India's premier online shopping marketplace for electronics, serving a retail universe of 3000+ cities. Supported by wide range of tailored marketing, data and service solutions, Nutify has more than 15,000 product variants and 500 brands and serves 3 million customers across the region.</p>
                
                <p>Nutify has more than 15,000+ monthly visitors, growing at a very fast. Nutify offers diverse assortment in categories ranging from highly curated to budget friendly.</p>
            </div>
            
            <div class="story-image">
                <img class="logoImg" src="/users/image/home/nutlog-removebg-preview.png" alt="nuttify">
            </div>
        </div>

        <!-- Services Section -->
        <div class="services-section">
            <div class="services-grid">
                <div class="service-item">
                    <div class="service-icon">🚚</div>
                    <div class="service-title">FREE AND FAST DELIVERY</div>
                    <div class="service-description">Free delivery for all orders over ₹500</div>
                </div>
                
                <div class="service-item">
                    <div class="service-icon">🎧</div>
                    <div class="service-title">24/7 CUSTOMER SERVICE</div>
                    <div class="service-description">Friendly 24/7 customer support</div>
                </div>
                
                <div class="service-item">
                    <div class="service-icon">✅</div>
                    <div class="service-title">MONEY BACK GUARANTEE</div>
                    <div class="service-description">We return money within 30 days</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Add smooth scroll and interaction effects
        document.addEventListener('DOMContentLoaded', function() {
            // Animate stats on scroll
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, observerOptions);

            // Observe stat cards
            document.querySelectorAll('.stat-card').forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(30px)';
                card.style.transition = 'all 0.6s ease';
                observer.observe(card);
            });

            // Add counter animation to numbers
            function animateCounter(element, target) {
                let current = 0;
                const increment = target / 50;
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= target) {
                        current = target;
                        clearInterval(timer);
                    }
                    
                    if (target >= 1000) {
                        element.textContent = (current / 1000).toFixed(1) + 'k';
                    } else {
                        element.textContent = Math.floor(current);
                    }
                }, 20);
            }

            // Trigger counter animation when visible
            const statNumbers = document.querySelectorAll('.stat-number');
            const targets = [10.5, 33, 25.5, 400];
            
            setTimeout(() => {
                statNumbers.forEach((num, index) => {
                    const target = targets[index] * (targets[index] < 100 ? 1000 : 1000);
                    animateCounter(num, target);
                });
            }, 500);
        });
    </script>
</body>
</html>
<%-include("../../views/partials/user/footer")%>
