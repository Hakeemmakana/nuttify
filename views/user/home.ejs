

<%-include("../../views/partials/user/header")%>
<style>

.container {
    max-width: 1200px;
    margin: 0 auto;
}

.section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
}

.red-bar {
    display: inline-block;
    background-color: #e74c3c;
    color: white;
    padding: 3px 8px;
    font-size: 14px;
    margin-bottom: 5px;
}

h2 {
    font-size: 24px;
    font-weight: bold;
    margin-top: 5px;
}

.view-all {
    background-color: #e74c3c;
    color: white;
    border: none;
    padding: 8px 15px;
    border-radius: 4px;
    font-size: 14px;
    cursor: pointer;
    text-decoration: none;
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 30px;
}

.product-card {
    position: relative;
    border-radius: 5px;
    overflow: hidden;
}

.product-image {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-radius: 5px;
}

.product-title {
    font-size: 14px;
    margin-top: 8px;
    font-weight: normal;
}

.product-price {
    color: #e74c3c;
    font-weight: bold;
    font-size: 14px;
    margin: 5px 0;
}

.product-original-price {
    text-decoration: line-through;
    color: #999;
    font-size: 14px;
    margin-left: 5px;
}

.stars {
    color: #f39c12;
    font-size: 14px;
    margin-right: 5px;
}

.reviews {
    color: #777;
    font-size: 12px;
}

.heart-icon {
    position: absolute;
    top: 10px;
    right: 10px;
    background-color: white;
    border-radius: 50%;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.rating-container {
    display: flex;
    align-items: center;
}

.deal-banner {
    display: flex;
    background-color: #000;
    border-radius: 8px;
    overflow: hidden;
    height: 200px;
    margin-top: 30px;
}

.deal-content {
    flex: 1;
    padding: 30px;
    color: white;
}

.deal-title {
    color: #e74c3c;
    font-size: 16px;
    margin-bottom: 15px;
}

.deal-heading {
    font-size: 26px;
    font-weight: bold;
    line-height: 1.2;
    margin-bottom: 20px;
}

.deal-options {
    display: flex;
    gap: 10px;
    margin-bottom: 20px;
}

.deal-option {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background-color: #333;
    border-radius: 50%;
    font-size: 12px;
}

.deal-value {
    font-weight: bold;
    margin-bottom: 2px;
}

.deal-label {
    font-size: 10px;
    color: #999;
}

.buy-button {
    background-color: #e74c3c;
    color: white;
    border: none;
    padding: 8px 20px;
    border-radius: 4px;
    font-size: 14px;
    cursor: pointer;
}

.deal-image {
    flex: 1;
    background-size: cover;
    background-position: center;
}
.banner {
    height: 190px;
}
a{
    text-decoration: none;
}



.ftco-section {
            padding: 80px 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
        }

        .ftco-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.95);
            z-index: 1;
        }

        .ftco-no-pt {
            padding-top: 0;
        }

        .ftco-no-pb {
            padding-bottom: 0;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
            position: relative;
            z-index: 2;
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
        }

        .row1 {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
            gap: 8px;
        }

        .no-gutters {
            margin: 0;
        }

        .no-gutters > [class*="col-"] {
            padding: 0;
        }

        .ftco-services {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            margin: 40px 0;
        }

        .col-lg-4 {
            flex: 0 0 33.333333%;
            max-width: 33.333333%;
            position: relative;
        }

        .col-lg-4:not(:last-child)::after {
            content: '';
            position: absolute;
            right: 0;
            top: 20%;
            bottom: 20%;
            width: 1px;
            background: linear-gradient(to bottom, transparent, #e0e0e0, transparent);
        }

        .text-center {
            text-align: center;
        }

        .d-flex {
            display: flex;
        }

        .align-self-stretch {
            align-self: stretch;
        }

        .justify-content-center {
            justify-content: center;
        }

        .align-items-center {
            align-items: center;
        }

        .ftco-animate {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        .ftco-animate:nth-child(1) {
            animation-delay: 0.2s;
        }

        .ftco-animate:nth-child(2) {
            animation-delay: 0.4s;
        }

        .ftco-animate:nth-child(3) {
            animation-delay: 0.6s;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .media {
            display: flex;
            flex-direction: column;
            width: 100%;
        }

        .block-6 {
            transition: all 0.3s ease;
        }

        .services {
            padding: 60px 40px;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .services::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, transparent 0%, rgba(102, 126, 234, 0.05) 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .services:hover::before {
            opacity: 1;
        }

        .services:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        .p-4 {
            padding: 1.5rem;
        }

        .py-md-5 {
            padding-top: 3rem;
            padding-bottom: 3rem;
        }

        .icon {
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            margin-bottom: 30px;
            position: relative;
            transition: all 0.3s ease;
        }

        .icon::before {
            content: '';
            position: absolute;
            top: -5px;
            left: -5px;
            right: -5px;
            bottom: -5px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 50%;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .services:hover .icon::before {
            opacity: 0.3;
        }

        .icon span {
            font-size: 40px;
            color: white;
            z-index: 2;
            position: relative;
        }

        /* Icon styles using pseudo-elements since flaticon fonts aren't available */
        .flaticon-bag::before {
            content: "🛍️";
            font-size: 35px;
        }

        .flaticon-customer-service::before {
            content: "🎧";
            font-size: 35px;
        }

        .flaticon-payment-security::before {
            content: "🔒";
            font-size: 35px;
        }

        .mb-4 {
            margin-bottom: 1.5rem;
        }

        .media-body {
            text-align: center;
            flex: 1;
        }

        .heading {
            font-size: 24px;
            font-weight: 700;
            color: #333;
            margin-bottom: 20px;
            position: relative;
            transition: color 0.3s ease;
        }

        .services:hover .heading {
            color: #667eea;
        }

        .media-body p {
            font-size: 16px;
            color: #666;
            line-height: 1.8;
            margin: 0;
            transition: color 0.3s ease;
        }

        .services:hover .media-body p {
            color: #555;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .col-lg-4 {
                flex: 0 0 100%;
                max-width: 100%;
            }

            .col-lg-4:not(:last-child)::after {
                display: none;
            }

            .col-lg-4:not(:last-child) {
                border-bottom: 1px solid #e0e0e0;
            }

            .ftco-services {
                margin: 20px 0;
            }

            .services {
                padding: 40px 30px;
            }
        }

        @media (max-width: 768px) {
            .ftco-section {
                padding: 40px 0;
            }

            .services {
                padding: 30px 20px;
            }

            .icon {
                width: 80px;
                height: 80px;
            }

            .icon span {
                font-size: 30px;
            }

            .flaticon-bag::before,
            .flaticon-customer-service::before,
            .flaticon-payment-security::before {
                font-size: 28px;
            }

            .heading {
                font-size: 20px;
            }

            .media-body p {
                font-size: 14px;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 10px;
            }

            .services {
                padding: 25px 15px;
            }

            .icon {
                width: 70px;
                height: 70px;
            }

            .heading {
                font-size: 18px;
                margin-bottom: 15px;
            }
        }
        
       
        .ftco-gallery {
            padding: 100px 0 50px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            position: relative;
            overflow: hidden;
        }

        .ftco-gallery::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.95);
            z-index: 1;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
            position: relative;
            z-index: 2;
        }

        .container-fluid {
            width: 100%;
            padding: 0;
            position: relative;
            z-index: 2;
        }

        .px-0 {
            padding-left: 0;
            padding-right: 0;
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            margin: 0 -15px;
        }

        .no-gutters {
            margin: 0;
        }

        .no-gutters > [class*="col-"] {
            padding: 0;
        }

        .justify-content-center {
            justify-content: center;
        }

        .col-md-8 {
            flex: 0 0 66.666667%;
            max-width: 66.666667%;
        }

        .col-md-4 {
            flex: 0 0 33.333333%;
            max-width: 33.333333%;
        }

        .col-lg-2 {
            flex: 0 0 25%;
            /* max-width: 20%; */
        }

        .heading-section {
            margin-bottom: 80px;
        }

        .text-center {
            text-align: center;
        }

        .mb-4 {
            margin-bottom: 2rem;
        }

        .ftco-animate {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.8s ease forwards;
        }

        .ftco-animate:nth-child(1) { animation-delay: 0.1s; }
        .ftco-animate:nth-child(2) { animation-delay: 0.2s; }
        .ftco-animate:nth-child(3) { animation-delay: 0.3s; }
        .ftco-animate:nth-child(4) { animation-delay: 0.4s; }
        .ftco-animate:nth-child(5) { animation-delay: 0.5s; }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .heading-section h2 {
            font-size: 48px;
            font-weight: 700;
            color: #333;
            margin-bottom: 30px;
            position: relative;
        }

        .heading-section h2::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 2px;
        }

        .heading-section p {
            font-size: 18px;
            color: #666;
            line-height: 1.8;
            max-width: 600px;
            margin: 0 auto;
        }

        .d-flex {
            display: flex;
        }

        .align-items-center {
            align-items: center;
        }

        .justify-content-center {
            justify-content: center;
        }

        .gallery-container {
            position: relative;
            width: 100%;
            aspect-ratio: 1;
            overflow: hidden;
            cursor: pointer;
            border: 3px solid transparent;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .gallery-container:hover {
            transform: scale(1.05);
            border-color: #667eea;
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.3);
        }

        .gallery {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: all 0.4s ease;
        }

        .gallery-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.4);
            opacity: 0;
            transition: all 0.4s ease;
            z-index: 1;
        }

        .gallery-container:hover::before {
            opacity: 1;
        }

        .gallery-container .icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0.8);
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 50%;
            z-index: 2;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            opacity: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .gallery-container:hover .icon {
            transform: translate(-50%, -50%) scale(1);
            opacity: 1;
        }

        .icon-instagram::before {
            content: "📷";
            font-size: 32px;
            color: #333;
        }

        .icon-instagram {
            position: relative;
        }

        .icon-instagram::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 40px;
            height: 40px;
            background: linear-gradient(45deg, #f09433 0%, #e6683c 25%, #dc2743 50%, #cc2366 75%, #bc1888 100%);
            border-radius: 12px;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .gallery-container:hover .icon-instagram::after {
            opacity: 1;
        }

        .gallery-container:hover .icon-instagram::before {
            color: white;
            z-index: 1;
            position: relative;
        }

        /* Modal styles */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.9);
            animation: fadeIn 0.3s ease;
        }

        .modal.active {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            max-width: 90%;
            max-height: 90%;
            border-radius: 20px;
            box-shadow: 0 40px 80px rgba(0, 0, 0, 0.5);
            animation: scaleIn 0.3s ease;
        }

        .modal-image {
            width: 100%;
            height: 100%;
            object-fit: contain;
            border-radius: 20px;
        }

        .close {
            position: absolute;
            top: 20px;
            right: 35px;
            color: #fff;
            font-size: 40px;
            font-weight: bold;
            cursor: pointer;
            z-index: 1001;
        }

        .close:hover {
            color: #ccc;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes scaleIn {
            from { transform: scale(0.7); }
            to { transform: scale(1); }
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .col-lg-2 {
                flex: 0 0 33.333333%;
                max-width: 33.333333%;
            }

            .heading-section h2 {
                font-size: 36px;
            }
        }

        @media (max-width: 768px) {
            .col-md-4,
            .col-lg-2 {
                flex: 0 0 50%;
                max-width: 50%;
            }

            .col-md-8 {
                flex: 0 0 100%;
                max-width: 100%;
            }

            .ftco-gallery {
                padding: 60px 0 30px;
            }

            .heading-section h2 {
                font-size: 28px;
            }

            .heading-section p {
                font-size: 16px;
            }

            .gallery-container .icon {
                width: 60px;
                height: 60px;
            }

            .icon-instagram::before {
                font-size: 24px;
            }

            .modal-content {
                max-width: 95%;
                max-height: 95%;
            }
        }

        @media (max-width: 480px) {
            .col-md-4,
            .col-lg-2 {
                flex: 0 0 100%;
                max-width: 100%;
            }

            .container {
                padding: 0 10px;
            }

            .gallery-container {
                margin-bottom: 1px;
            }
        }


        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Sidebar Styling */
        .sidebar {
            background: linear-gradient(145deg, #ffffff, #f8f9fa);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            border: 1px solid rgba(255,255,255,0.2);
            position: sticky;
            top: 2rem;
            height: fit-content;
            

        }

        .sidebar h5 {
            color: #2c3e50;
            font-weight: 700;
            margin-bottom: 1.5rem;
            position: relative;
            padding-bottom: 0.5rem;
        }

        .sidebar h5::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 3px;
            background: linear-gradient(90deg, #ff6b6b, #ee5a24);
            border-radius: 2px;
        }

        .category-list {
            list-style: none;
            padding: 0;
        }

        .category-list li {
            margin-bottom: 0.5rem;
            transform: translateX(0);
            transition: all 0.3s ease;
        }

        .category-list li a {
            display: flex;
            align-items: center;
            padding: 0.8rem 1rem;
            color: #555;
            text-decoration: none;
            border-radius: 12px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .category-list li a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,107,107,0.1), transparent);
            transition: left 0.5s ease;
        }

        .category-list li a:hover::before {
            left: 100%;
        }

        .category-list li a:hover {
            color: #ff6b6b;
            background: rgba(255,107,107,0.1);
            transform: translateX(5px);
            box-shadow: 0 5px 15px rgba(255,107,107,0.3);
        }

        .category-list li a::after {
            content: '→';
            opacity: 0;
            transform: translateX(-10px);
            transition: all 0.3s ease;
            margin-left: auto;
        }

        .category-list li a:hover::after {
            opacity: 1;
            transform: translateX(0);
        }

        /* Banner Styling */
        .banner {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 25px;
            padding: 3rem;
            color: white;
            position: relative;
            overflow: hidden;
            min-height: 300px;
            display: flex;
            align-items: center;
            box-shadow: 0 25px 50px rgba(0,0,0,0.2);
        }

        .banner::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .banner-content {
            position: relative;
            z-index: 2;
            max-width: 50%;
        }

        .banner-content h6 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            animation: slideInLeft 1s ease;
        }

        @keyframes slideInLeft {
            0% { transform: translateX(-100px); opacity: 0; }
            100% { transform: translateX(0); opacity: 1; }
        }

        .voucher-text {
            font-size: 1.2rem;
            margin-bottom: 1.5rem;
            opacity: 0.9;
            animation: slideInLeft 1s ease 0.3s both;
        }

        .shop-now-btn {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
            padding: 1rem 2rem;
            border: none;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            box-shadow: 0 10px 30px rgba(255,107,107,0.4);
            animation: slideInLeft 1s ease 0.6s both;
        }

        .shop-now-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(255,107,107,0.6);
            color: white;
        }

        .shop-now-btn i {
            transition: transform 0.3s ease;
        }

        .shop-now-btn:hover i {
            transform: translateX(5px);
        }

        /* Indicator Dots */
        .indicator {
            display: flex;
            justify-content: center;
            gap: 0.5rem;
        }

        .dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #ddd;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .dot.active {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            transform: scale(1.2);
        }

        .dot:hover {
            background: #ff6b6b;
            transform: scale(1.1);
        }

        /* Flash Sales Section */
        .flash-sales {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            margin-top: 2rem;
        }

        .flash-sale-header {
            display: flex;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .flash-icon {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .flash-sale-header h5 {
            color: #2c3e50;
            font-weight: 700;
            margin: 0 2rem 0 1rem;
        }

        .countdown {
            display: flex;
            gap: 0.5rem;
            margin-left: auto;
        }

        .countdown-item {
            background: #2c3e50;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-weight: 600;
            min-width: 50px;
            text-align: center;
            animation: countdownPulse 1s infinite;
        }

        @keyframes countdownPulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* Product Cards */
        .product-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
            position: relative;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.2);
        }

        .product-card .position-relative {
            position: relative;
            overflow: hidden;
        }

        .product-img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            transition: all 0.3s ease;
        }

        .product-card:hover .product-img {
            transform: scale(1.1);
        }

        .sale-badge {
            position: absolute;
            top: 1rem;
            left: 1rem;
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            color: white;
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            z-index: 2;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        .favorite-btn {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: rgba(255,255,255,0.9);
            color: #ff6b6b;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            transition: all 0.3s ease;
            z-index: 2;
            backdrop-filter: blur(10px);
        }

        .favorite-btn:hover {
            background: #ff6b6b;
            color: white;
            transform: scale(1.1);
        }

        .product-info {
            padding: 1.5rem;
        }

        .product-info h6 {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }

        .price-container {
            margin-bottom: 0.5rem;
        }

        .current-price {
            font-size: 1.3rem;
            font-weight: 700;
            color: #ff6b6b;
        }

        .old-price {
            font-size: 1rem;
            color: #999;
            text-decoration: line-through;
            margin-left: 0.5rem;
        }

        .ratings {
            display: flex;
            align-items: center;
            gap: 0.2rem;
        }

        .ratings i {
            font-size: 0.9rem;
        }

        /* View All Button */
        .view-all-btn {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 1rem 3rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(102,126,234,0.4);
        }

        .view-all-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(102,126,234,0.6);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 1rem;
            }
            
            .banner-content {
                max-width: 100%;
            }
            
            .banner-content h6 {
                font-size: 1.8rem;
            }
            
            .flash-sale-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            
            .countdown {
                margin-left: 0;
            }
            
            .sidebar {
                margin-bottom: 2rem;
            }
        }

        /* Loading Animation */
        .loading {
            opacity: 0;
            animation: fadeInUp 0.6s ease forwards;
        }

        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Interactive Hover Effects */
        .product-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: left 0.5s ease;
            z-index: 1;
        }

        .product-card:hover::before {
            left: 100%;
        }


        .showcase__wrapper {
            max-width: 1200px;
            margin: 0 auto;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            padding: 40px;
        }

        .showcase__title_section {
            text-align: center;
            margin-bottom: 50px;
        }

        .showcase__main_heading {
            font-size: 3rem;
            color: #333;
            margin-bottom: 10px;
            background: linear-gradient(45deg, #8B4513, #D2691E);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .showcase__subtitle_text {
            font-size: 1.2rem;
            color: #666;
        }

        .showcase__items_display {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 25px;
            margin-bottom: 60px;
        }

        .showcase__product_card {
            background: white;
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .showcase__product_card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
            border: 2px solid #667eea;
        }

        .showcase__item_image {
            width: 120px;
            height: 120px;
            border-radius: 15px;
            margin: 0 auto 20px;
            transition: all 0.3s ease;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .showcase__item_image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 15px;
        }

        .showcase__product_card:hover .showcase__item_image {
            transform: scale(1.05);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .showcase__item_name {
            font-size: 1.3rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 10px;
        }



        .showcase__quick_buy {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s ease;
            opacity: 0;
            transform: translateY(20px);
        }

        .showcase__product_card:hover .showcase__quick_buy {
            opacity: 1;
            transform: translateY(0);
        }

        .showcase__quick_buy:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .showcase__cta_section {
            text-align: center;
            margin-top: 40px;
        }

        .showcase__main_button {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 20px 50px;
            font-size: 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }

        .showcase__main_button:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(102, 126, 234, 0.4);
            background: linear-gradient(45deg, #764ba2, #667eea);
        }

        .showcase__main_button:active {
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .showcase__items_display {
                grid-template-columns: repeat(2, 1fr);
                gap: 20px;
            }
            
            .showcase__main_heading {
                font-size: 2rem;
            }
            
            .showcase__wrapper {
                padding: 20px;
            }
        }

        @media (max-width: 480px) {
            .showcase__items_display {
                grid-template-columns: 1fr;
            }
        }
</style>
    

    <!-- Main Content -->
    <div class="container1 mt-4">
        <div class="row">
            <!-- Categories Sidebar -->
            <div class="col-md-3">
                <div class="sidebar loading">
                    <h5 class="mb-3">Product Categories</h5>
                    <ul class="category-list">
                        <%for(let i=0;i<=category.length;i++){%>
                        
                        <%}%>
                        <li><a href="#"></i>Dates</a></li>
                        <li><a href="#"></i>Dry Fruits</a></li>
                        <li><a href="#">Raisins</a></li>
                        <li><a href="#"></i>Nuts</a></li>
                        <li><a href="#">Seeds</a></li>
                        <li><a href="#"></i>Spices</a></li>
                        <li><a href="#"></i>Chocolates</a></li>
                    </ul>
                </div>
            </div>
            
            <!-- Main Content Area -->
            <div class="col-md-9">
                <!-- Banner -->
                <div class="banner mb-4 loading" style="background-image:url('users/image/home/bannerMabroom.jpg');">
                    <div class="banner-content">
                        <h6>Medjool Jumbo Dates</h6>
                        <div class="voucher-text">Up to 10% off Voucher</div>
                        <a href="#" class="shop-now-btn mt-2">
                            Shop Now <i class="fas fa-arrow-right ms-2"></i>
                        </a>
                    </div>
                </div>
                
                <!-- Indicator Dots -->
                <div class="indicator mb-4 loading">
                    <span class="dot active"></span>
                    <span class="dot"></span>
                    <span class="dot"></span>
                    <span class="dot"></span>
                    <span class="dot"></span>
                </div>
                
                <!-- Flash Sales Section -->
                <div class="flash-sales mt-5 loading">
                    <div class="flash-sale-header">
                        <div class="flash-icon">Today's</div>
                        <h5 class="mb-0 ms-2">Flash Sales</h5>
                        <div class="countdown">
                            <div class="countdown-item">03</div>
                            <div class="countdown-item">23</div>
                            <div class="countdown-item">19</div>
                            <div class="countdown-item">56</div>
                        </div>
                    </div>
                    
                    <!-- Products Grid -->
                     <!-- kkkkkkkkkkkkkkkk -->
                    <div class="row">
                        <!-- Product Loop -->
                        <%for(let i=0;i<4;i++){%>
                        <div class="col-md-3 col-sm-6">
                            <div class="product-card loading">
                                <div class="position-relative">
                                    
                                    <!-- <a class="favorite-btn" href="/addToWishlist?id=<%=products[i]._id%>">
                                        <i class="far fa-heart"></i>
                                    </a> -->
                                    <a href="/productDetails?id=<%=products[i]._id%>">
                                        <img src="<%=products[i].productImage[0]%>" alt="<%=products[i].productName%>" class="product-img">
                                    </a>
                                </div>
                                <div class="product-info">
                                    <h6><%=products[i].productName%></h6>
                                    <div class="price-container">
                                        <span class="current-price">₹<%=products[i].regularPrice%></span>
                                        <span class="old-price">₹000</span>
                                    </div>
                                    <div class="ratings">
                                        <i class="fas fa-star text-warning"></i>
                                        <i class="fas fa-star text-warning"></i>
                                        <i class="fas fa-star text-warning"></i>
                                        <i class="fas fa-star text-warning"></i>
                                        <i class="fas fa-star text-warning"></i>
                                        <small class="text-muted">(56)</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <%}%>
                    </div>
                    
                    <!-- View All Button -->
                    <div class="text-center mt-4 mb-5">
                        <a href="/shoap">
                            <button class="view-all-btn">View All Products</button>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
<section class="ftco-section ftco-no-pt ftco-no-pb">
    <div class="container">
        <div class="row no-gutters ftco-services">
            <div class="col-lg-4 text-center d-flex align-self-stretch ftco-animate">
                <div class="media block-6 services p-4 py-md-5">
                    <div class="icon d-flex justify-content-center align-items-center mb-4">
                        <span class="flaticon-bag"></span>
                    </div>
                    <div class="media-body">
                        <h3 class="heading">Free Shipping</h3>
                        <p>Every package tells a story, and every gift is a treasure waiting to be unwrapped.</p>
                    </div>
                </div>      
            </div>
            <div class="col-lg-4 text-center d-flex align-self-stretch ftco-animate">
                <div class="media block-6 services p-4 py-md-5">
                    <div class="icon d-flex justify-content-center align-items-center mb-4">
                        <span class="flaticon-customer-service"></span>
                    </div>
                    <div class="media-body">
                        <h3 class="heading">Support Customer</h3>
                        <p>Here for you, every hour, every day—because your satisfaction never clocks out</p>
                    </div>
                </div>    
            </div>
            <div class="col-lg-4 text-center d-flex align-self-stretch ftco-animate">
                <div class="media block-6 services p-4 py-md-5">
                    <div class="icon d-flex justify-content-center align-items-center mb-4">
                        <span class="flaticon-payment-security"></span>
                    </div>
                    <div class="media-body">
                        <h3 class="heading">Secure Payments</h3>
                        <p>Shop with confidence—your trust and safety are our top priorities.</p>
                    </div>
                </div>      
            </div>
        </div>
    </div>
</section>

  

        <!-- Deal of the Day Banner -->
        
    </div>
   

    <div class="showcase__wrapper">
        <div class="showcase__title_section">
            <h1 class="showcase__main_heading">Premium Nuts & Dry Fruits</h1>
            <p class="showcase__subtitle_text">Fresh, Natural & Healthy - Handpicked for Quality</p>
        </div>

        <div class="showcase__items_display">
            
            <!-- First Row -->
            <% const limit = Math.min(products.length, 8); %>
            <%for(let i=0;i<limit;i++){%>
            <div class="showcase__product_card">
                <div class="showcase__item_image">
                    <img src="<%=products[i].productImage[0]%>" alt="Premium Almonds">
                </div>
                <h3 class="showcase__item_name"><%=products[i].productName%></h3>
                <button class="showcase__quick_buy">Add to Cart</button>
            </div>
        <%}%>
            
           

            
        </div>

        <div class="showcase__cta_section">
            <button class="showcase__main_button" onclick="handleShopNow()">Shop Now</button>
        </div>
    </div>
    
        

   
    <!-- <a href="/shoap"><button class="view-all-btn">View All Products</button></a> -->


    <section class="ftco-gallery">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-8 heading-section text-center mb-4 ftco-animate">
                    <h2 class="mb-4">Follow Us On Instagram</h2>
                    <p>Far far away, behind the word mountains, far from the countries Vokalia and Consonantia, there live the blind texts. Separated they live in</p>
                </div>
            </div>
        </div>
        <div class="container-fluid px-0">
            <div class="row no-gutters justify-content-center">
                <div class="col-md-4 col-lg-2 ftco-animate">
                    <div class="gallery-container">
                        <img src="/users/image/home/homeLast/karakka.webp" alt="Instagram Gallery Image 1" class="gallery">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="icon-instagram"></span>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-lg-2 ftco-animate">
                    <div class="gallery-container">
                        <img src="/users/image/home/homeLast/pistha.avif" alt="Instagram Gallery Image 2" class="gallery">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="icon-instagram"></span>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-lg-2 ftco-animate">
                    <div class="gallery-container">
                        <img src="/users/image/home/homeLast/kivi.webp" alt="Instagram Gallery Image 3" class="gallery">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="icon-instagram"></span>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-lg-2 ftco-animate">
                    <div class="gallery-container">
                        <img src="/users/image/home/homeLast/unshelledpistha.webp" alt="Instagram Gallery Image 4" class="gallery">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="icon-instagram"></span>
                        </div>
                    </div>
                </div>
                <div class="col-md-4 col-lg-2 ftco-animate">
                    <div class="gallery-container">
                        <img src="/users/image/home/homeLast/adiparipp.webp" alt="Instagram Gallery Image 5" class="gallery">
                        <div class="icon d-flex align-items-center justify-content-center">
                            <span class="icon-instagram"></span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
    </section>

    <!-- Modal for image popup -->
    <div id="imageModal" class="modal">
        <span class="close">&times;</span>
        <div class="modal-content">
            <img id="modalImage" class="modal-image" src="" alt="Gallery Image">
        </div>
    </div>

  
      
    <!-- Scripts -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Simple script to handle favorite buttons
        document.querySelectorAll('.favorite-btn').forEach(button => {
            button.addEventListener('click', function() {
                const icon = this.querySelector('i');
                if (icon.classList.contains('far')) {
                    icon.classList.remove('far');
                    icon.classList.add('fas');
                    icon.style.color = '#f44336';
                } else {
                    icon.classList.remove('fas');
                    icon.classList.add('far');
                    icon.style.color = '';
                }
            });
        });
    </script> -->

    <script>
 document.addEventListener('DOMContentLoaded', function() {
            const galleryContainers = document.querySelectorAll('.gallery-container');
            const modal = document.getElementById('imageModal');
            const modalImage = document.getElementById('modalImage');
            const closeBtn = document.querySelector('.close');

            // Add click event to each gallery container
            galleryContainers.forEach(container => {
                container.addEventListener('click', function() {
                    const img = this.querySelector('.gallery');
                    modal.classList.add('active');
                    modalImage.src = img.src;
                    modalImage.alt = img.alt;
                    document.body.style.overflow = 'hidden';
                });
            });

            // Close modal functions
            function closeModal() {
                modal.classList.remove('active');
                document.body.style.overflow = 'auto';
            }

            // Close on X button click
            closeBtn.addEventListener('click', closeModal);

            // Close on background click
            modal.addEventListener('click', function(e) {
                if (e.target === modal) {
                    closeModal();
                }
            });

            // Close on ESC key
            document.addEventListener('keydown', function(e) {
                if (e.key === 'Escape' && modal.classList.contains('active')) {
                    closeModal();
                }
            });

            // Intersection Observer for animations
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };

            const observer = new IntersectionObserver(function(entries) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animationPlayState = 'running';
                    }
                });
            }, observerOptions);

            // Observe all animated elements
            document.querySelectorAll('.ftco-animate').forEach(el => {
                el.style.animationPlayState = 'paused';
                observer.observe(el);
            });

            // Add staggered hover effects
            galleryContainers.forEach((container, index) => {
                container.addEventListener('mouseenter', function() {
                    galleryContainers.forEach((c, i) => {
                        if (Math.abs(i - index) <= 1 && c !== container) {
                            c.style.transform = 'scale(0.95)';
                            c.style.opacity = '0.7';
                        }
                    });
                });

                container.addEventListener('mouseleave', function() {
                    galleryContainers.forEach(c => {
                        c.style.transform = '';
                        c.style.opacity = '';
                    });
                });
            });
        });


        function animateOnScroll() {
            const elements = document.querySelectorAll('.loading');
            elements.forEach((element, index) => {
                setTimeout(() => {
                    element.style.animationDelay = `${index * 0.1}s`;
                }, index * 100);
            });
        }

        // Initialize animations
        document.addEventListener('DOMContentLoaded', animateOnScroll);

        // Interactive dots functionality
        const dots = document.querySelectorAll('.dot');
        dots.forEach((dot, index) => {
            dot.addEventListener('click', () => {
                dots.forEach(d => d.classList.remove('active'));
                dot.classList.add('active');
            });
        });

        // Add to wishlist animation
        document.querySelectorAll('.favorite-btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                e.preventDefault();
                const icon = this.querySelector('i');
                icon.classList.toggle('far');
                icon.classList.toggle('fas');
                
                // Add animation effect
                this.style.transform = 'scale(1.3)';
                setTimeout(() => {
                    this.style.transform = 'scale(1)';
                }, 200);
                
                // Navigate to wishlist URL
                setTimeout(() => {
                    window.location.href = this.href;
                }, 300);
            });
        });

        // Countdown timer functionality
        function updateCountdown() {
            const countdownItems = document.querySelectorAll('.countdown-item');
            countdownItems.forEach(item => {
                let currentValue = parseInt(item.textContent);
                if (currentValue > 0) {
                    currentValue--;
                    item.textContent = currentValue.toString().padStart(2, '0');
                }
            });
        }

        // Update countdown every second
        setInterval(updateCountdown, 1000);

        // Add smooth scrolling for category links
        document.querySelectorAll('.category-list a').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                // Add active state
                document.querySelectorAll('.category-list a').forEach(l => l.classList.remove('active'));
                this.classList.add('active');
            });
        });
    </script>
    <%-include("../../views/partials/user/footer")%>